<!DOCTYPE html>
<html>
  <head>
    <title>Yahw</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column">
    <%= render 'layouts/navbar' %> <!-- Won't work if `layouts/` isn't added. -->
    <div id="page-content">
      <%= render 'layouts/messages' %> <!-- Won't work if `layouts/` isn't added. -->
      <%= yield %>
    </div>
    <%= render 'layouts/footer' %> <!-- Won't work if `layouts/` isn't added. -->
  </body>
</html>
